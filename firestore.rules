rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para desarrollo: permitir acceso completo
    // IMPORTANTE: Estas reglas son adecuadas para desarrollo pero NO para producción
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Reglas para producción (comentadas)
    // Descomenta y personaliza estas reglas cuando estés listo para producción
    
    // Regla para el estado de la aplicación
    // match /app_state/{docId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    
    // Regla para usuarios
    // match /users/{userId} {
    //   allow read: if true;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
    
    // Regla para menús
    // match /menus/{menuId} {
    //   allow read: if true;
    //   allow write: if request.auth != null && request.resource.data.createdBy == request.auth.uid;
    // }
    
    // Regla para coordinadores
    // match /coordinators/{coordinatorId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    
    // Regla para confirmaciones
    // match /confirmations/{confirmationId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    
    // Regla para confirmaciones de asistencia
    // match /attendance_confirmations/{confirmationId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}
